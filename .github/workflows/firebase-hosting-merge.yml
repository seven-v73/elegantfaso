# This file was auto-generated by the Firebase CLI and is kept only as a
# manual hosting hook. The mobile release gate is .github/workflows/flutter-ci.yml.

name: Manual Firebase Hosting Deploy
on: workflow_dispatch
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FASOSTYLE_1BB74 }}
          channelId: live
          projectId: fasostyle-1bb74
